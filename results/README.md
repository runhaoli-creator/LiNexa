# results

- `open_loop/` — open-loop eval outputs (JSON/CSV).
- `closed_loop/` — closed-loop rollout outputs.
- `figures/` — plots used in writeups.

Large artifacts should be gitignored; keep only summary tables and figures under version control.
