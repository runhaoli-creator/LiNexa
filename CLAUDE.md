# LiNexa

## Overview
<!-- TODO: 1-3 sentences on what this project is investigating. -->
ML research project. Venue/deadline TBD.

## Tech Stack
- Python 3.x (check `venv/` or `pyproject.toml` before installing packages)
- PyTorch for ML
- <!-- TODO: add key libraries (transformers, hydra, wandb, etc.) as they're adopted -->

## Project Structure
```
LiNexa/
  .claude/          # agents, settings
  CLAUDE.md         # this file
  STATUS.md         # current phase, next actions
```
<!-- TODO: update as src/, configs/, scripts/, etc. are created -->

## Key Commands
<!-- TODO: fill in once train/eval entry points exist, e.g. -->
<!-- - Train: `python train.py config=base` -->
<!-- - Eval:  `python eval.py checkpoint=outputs/run1/best.ckpt` -->

## Conventions
- Type hints on function signatures
- f-strings over `.format()`
- Follow existing style; don't reformat untouched code
- Don't add docstrings/comments to code unless asked
- Never auto-push; never amend without asking

## Data / Artifacts
- `outputs/`, `checkpoints/`, `wandb/` are gitignored
- <!-- TODO: note dataset locations, shared storage paths, etc. -->

## References
<!-- TODO: related papers, baselines, internal docs -->
